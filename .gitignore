# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — the server reads its Mongo URI and JWT secret from here.
# Only .env.example is committed; never the real .env.
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example

# MCP config carries a live API key — keep it local.
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
